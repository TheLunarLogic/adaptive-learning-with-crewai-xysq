# Backend API URL — set this in Vercel environment variables.
# In local dev this is NOT needed; Vite proxy handles /api → localhost:8000.
# In production set it to your Railway backend URL, e.g.:
# VITE_API_URL=https://your-app.up.railway.app/api
VITE_API_URL=
